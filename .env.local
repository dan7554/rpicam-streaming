# Local development environment configuration for broadcast system
# This file is for local testing with custom hostnames

# Broadcast System Configuration
BROADCAST_HOSTNAME=local.broadcast.com
BROADCAST_PORT=443
NODE_ENV=production
PORT=3001

# MediaMTX Configuration
MEDIAMTX_URL=https://mediamtx:8889
